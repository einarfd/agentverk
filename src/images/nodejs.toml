# Node.js 22 LTS mixin.
# Installs Node.js system-wide via NodeSource.
# Required by the gemini and openclaw mixins, and useful for any project
# that needs a modern Node.js runtime.
# Use via: include = ["nodejs"]

[os_families.debian]
[[os_families.debian.setup]]
run = "curl -fsSL https://deb.nodesource.com/setup_22.x | bash - && apt-get install -y nodejs"

[os_families.fedora]
[[os_families.fedora.setup]]
run = "curl -fsSL https://rpm.nodesource.com/setup_22.x | bash - && dnf install -y nodejs"
