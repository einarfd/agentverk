# Zsh mixin.
# Installs zsh and sets it as the default shell for the VM user.
# Use via: include = ["zsh"]

[[setup]]
run = "apt-get update && apt-get install -y zsh && chsh -s $(which zsh) {{AGV_USER}}"
