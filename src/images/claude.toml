# Claude CLI mixin.
# Installs the Claude AI coding agent CLI.
# Requires curl — pair with devtools: include = ["devtools", "claude"]

[[provision]]
run = "curl -fsSL https://claude.ai/install.sh | bash"
