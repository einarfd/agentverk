# OpenAI Codex CLI AI coding agent VM.
# Uses the official Rust binary — no Node.js required.
#
# Equivalent command-line:
#   agv create --include devtools --include codex <name>

[base]
from = "ubuntu-24.04"
include = ["devtools", "codex"]

[vm]
memory = "4G"
cpus = 2
disk = "20G"
